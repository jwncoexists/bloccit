<h1>Comment</h1>
<div class="row">
  <div class="span4">
    <% if can? :destroy, @post %>
      <%= link_to "Delete Comment", [@topic, @post, @comment], method: :delete, class: 'btn btn-primary btn-mini btn-danger', confirm: 'Are you sure you want to delete this comment?' %>
      <br>
    <% end %>    
    <h5>Instructions for Editing Comments:</h5>
    <ul>
      <li>Modify your comment text on the right and click save.</li>
      <li>Comments must be a minimum of 5 characters long.</li>
      <% if can? :destroy, @post %>
        <li>To remove this comment, click the Delete button.</li>
      <% end %>
    </ul>
  </div>
  <div class="span8">
    <%= render partial: 'form', locals: { topic: @topic, post: @post, comment: @comment } %>
  </div>
</div>
